/*
let array=[]
let maior = 0

for (let cont=0; cont<20;cont++){
    array.push(parseInt(Math.random()*99))
    if (array[cont]>maior)
        maior=array[cont]
}
console.log(array)
console.log(maior) */